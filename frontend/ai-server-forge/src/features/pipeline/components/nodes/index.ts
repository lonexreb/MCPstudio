import ToolNode from './ToolNode';
import InputNode from './InputNode';
import OutputNode from './OutputNode';

export const nodeTypes = {
  tool: ToolNode,
  input: InputNode,
  output: OutputNode,
};

export { ToolNode, InputNode, OutputNode };
